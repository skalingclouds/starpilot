import './globals.css'
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Starpilot Web - AI-Powered GitHub Stars Search',
  description: 'Rediscover your GitHub stars with AI-powered semantic search. Find relevant repositories from your starred collection using natural language queries.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}